name: depend
on:
    workflow_run:
        workflows: [trigger]
        types:
          - completed
    

jobs:
  depent-job:
    name: DEPEND
    runs-on: ubuntu-latest

    steps:
      - name: echo
      run: echo "depend value = ${{ github.event.workflow_run.value }}"
